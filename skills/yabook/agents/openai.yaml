interface:
  display_name: "YABook"
  short_description: "Padrões YA LABS para GitHub, docs e IA"
  default_prompt: "Use $yabook do para criar issues, branches, PRs, releases ou merges seguindo os padrões YA LABS. Encadeie comandos com &, como $yabook load & commit msg."

policy:
  allow_implicit_invocation: true
